package Tests;

import model.map.MapEditor;
import org.junit.jupiter.api.Test;
import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.InputStream;
import java.io.PrintStream;
import java.util.Scanner;
import static org.junit.jupiter.api.Assertions.assertEquals;

public class MapEditorTest {

    @Test
    void testInitializeMapWithDots() {
        MapEditor editor = new MapEditor(5, 5);
        for (int i = 0; i < 5; i++) {
            for (int j = 0; j < 5; j++) {
                assertEquals('.', editor.getMap()[i][j], "Карта должна быть инициализирована точками");
            }
        }
    }

    @Test
    void testEditMapSymbol() {
        MapEditor editor = new MapEditor(5, 5);
        editor.editMap(0, 0, 'X');
        assertEquals('X', editor.getMap()[0][0], "Символ должен быть изменен");
    }

    @Test
    void testEditMapInvalidCoordinates() {
        MapEditor editor = new MapEditor(5, 5);
        editor.editMap(10, 10, 'X');
        assertEquals('.', editor.getMap()[0][0], "Символ не должен быть изменен из-за некорректных координат");
    }

    @Test
    void testEditMapInvalidSymbol() {
        MapEditor editor = new MapEditor(5, 5);
        editor.editMap(0, 0, ' ');
        assertEquals('.', editor.getMap()[0][0], "Символ не должен быть изменен из-за некорректного символа");
    }
}
/*
testInitializeMapWithDots():
Создает объект MapEditor с размерами 5x5.
Проходит по всем клеткам карты (двойной цикл).
Для каждой клетки проверяет, что ее значение равно ‘.’ (точкой).
Использует assertEquals('.', editor.getMap()[i][j], "Карта должна быть инициализирована точками") для проверки. Если значение ячейки не равно ‘.’, тест завершится ошибкой с сообщением «Карта должна быть инициализирована точками».
Цель теста: проверить, что при создании нового объекта MapEditor карта инициализируется точками во всех клетках.
testEditMapSymbol():

Создает объект MapEditor с размерами 5x5.
Вызывает метод editMap(0, 0, 'X'), чтобы изменить значение ячейки с координатами (0, 0) на символ ‘X’.
Проверяет, что значение ячейки с координатами (0, 0) действительно равно ‘X’.
Использует assertEquals('X', editor.getMap()[0][0], "Символ должен быть изменен") для проверки. Если значение ячейки не равно ‘X’, тест завершится с ошибкой и сообщением «Символ должен быть изменён».
Цель теста: проверить, что метод editMap() правильно изменяет значение ячейки на карте.
testEditMapInvalidCoordinates():

Создает объект MapEditor с размерами 5x5.
Вызывает метод editMap(10, 10, 'X'), чтобы изменить значение ячейки с координатами (10, 10) на символ ‘X’. Обратите внимание, что координаты (10, 10) находятся за пределами карты (5x5).
Проверяет, что значение ячейки с координатами (0, 0) осталось равным ‘.’ (точке). То есть тест проверяет, что попытка изменить ячейку с некорректными координатами не приводит к изменению других ячеек карты.
Использует assertEquals('.', editor.getMap()[0][0], "Символ не должен быть изменен из-за некорректных координат") для проверки. Если значение ячейки не равно ‘.’, тест завершится ошибкой с сообщением «Символ не должен быть изменен из-за некорректных координат».
Цель теста: проверить, что метод editMap() корректно обрабатывает некорректные координаты и не позволяет изменять клетки за пределами карты.
testEditMapInvalidSymbol():

Создает объект MapEditor с размерами 5x5.
Вызывает метод editMap(0, 0, ' '), чтобы изменить значение ячейки с координатами (0, 0) на символ ’ ’ (пробел). Предположительно, пробел является некорректным символом для карты.
Проверяет, что значение ячейки с координатами (0, 0) осталось равным ‘.’ (точке). То есть тест проверяет, что попытка изменить ячейку на некорректный символ не приводит к изменению значения ячейки.
Использует assertEquals('.', editor.getMap()[0][0], "Символ не должен быть изменен из-за некорректного символа") для проверки. Если значение ячейки не равно ‘.’, тест завершится ошибкой с сообщением «Символ не должен быть изменен из-за некорректного символа».
Цель теста: проверить, что метод editMap() корректно обрабатывает некорректные символы и не позволяет изменять ячейки на недопустимые значения.*/